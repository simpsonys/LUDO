feat: 파일 전사 스트리밍 및 진행률 표시 구현

- 대용량 파일 전사 시 UI가 멈추는 문제를 해결하기 위해 실시간 스트리밍 아키텍처를 구현했습니다.
- Python 워커가 전사 결과를 실시간으로 스트리밍하고, Rust 백엔드가 이를 받아 프론트엔드로 전달합니다.
- 전사 진행률(%)이 `Backend State`에 실시간으로 표시됩니다.
- `StreamHandle` 타입을 공유 스키마로 이동하고, `error` 이벤트에 `at` 타임스탬프를 추가했습니다.

fix: Rust 컴파일 오류 수정

- `tauri::Emitter` 트레이트를 import하여 `emit` 메소드 사용 오류를 해결했습니다.
- `child.wait().await` 호출을 `child.wait()`로 수정하여 비동기 관련 컴파일 오류를 해결했습니다.